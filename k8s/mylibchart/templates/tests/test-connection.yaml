apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mylibchart.fullname" . }}-test-connection"
  labels:
    {{- include "mylibchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mylibchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
